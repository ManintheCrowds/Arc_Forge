# Stage 1 — task decomposition (editable prompt)

This file is appended to `task_decomposition.md` when present. Edit it to tune how the GUI/agent should interpret storyboard structure without changing Python.

- Prefer **clear encounter names** that match your table’s combat/social beats.
- **Opportunities** are optional hooks for non-combat scenes; add them in YAML after generation if needed.
- Keep **sequence constraints** human-readable; they feed later stages.
