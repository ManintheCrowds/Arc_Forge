---
title: "Session handoff snapshot"
tags: ["type/harness-session-snapshot", "status/mirror", "domain/harness"]
group: harness-session
color: violet
cssclasses:
  - vault-grp-harness-session
  - vault-col-violet

---

# Session handoff snapshot

_Generated by sync_harness_to_vault.ps1 at 2026-04-17T05:44:47.0716632Z (UTC). Downstream audit copy; SSOT is `MiscRepos/.cursor/state/handoff_latest.md`._

---

# Handoff #18: Vault harness entry point, mirror metadata, resync wiring, security hardening

decision_id: handoff-2026-04-16-vault-harness-mirror-security
supersedes: handoff-2026-04-17-oa-fr3-vault-decision-index
Updated: 2026-04-16T22:45:00Z

## Done

- **Single entry:** [`local-proto/scripts/Sync-VaultHarness.ps1`](../../local-proto/scripts/Sync-VaultHarness.ps1), root [`package.json`](../../package.json) script **`npm run vault:sync`**; pointers in [`.cursor/docs/OBSIDIAN_VAULT_INTEGRATION.md`](../docs/OBSIDIAN_VAULT_INTEGRATION.md), [`.cursor/HANDOFF_FLOW.md`](../HANDOFF_FLOW.md), [`.cursor/docs/COMMANDS_README.md`](../docs/COMMANDS_README.md).
- **Mirror age in vault:** `sync_harness_to_vault.ps1` writes **`Harness/Mirror-Status.md`** and **`Harness/.mirror-meta.json`** (UTC, git short SHA, trigger, harness root); **`-SyncTrigger`** (`manual` / `Handoff` / `GitHook` / `Scheduler`); `int-vault-resync.ps1` passes trigger into sync.
- **Fingerprint:** `int-vault-resync` fingerprint includes **all** `.cursor/state/handoff_archive/*.md` (name + mtime + size) so archive-only flows invalidate sync; **`archive_handoff_latest.py`** runs **`int-vault-resync` (Handoff)** after archive.
- **Task flow:** [`.cursor/scripts/vault_resync.py`](../scripts/vault_resync.py) shared helper; **`write_handoff.py`** uses it; **`split_done_tasks_to_completed.py`** runs resync (**Scheduler**) after writes + doc/header updates; **`validate_pending_tasks_table.py`** docstring reminds vault refresh.
- **Security (prior audit P0–P4):** **`$global:LASTEXITCODE = 0`** only immediately after **`git rev-parse`** in sync (not at script end); **`vault_resync._resolve_powershell_exe`** → `POWERSHELL_EXE` then `%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe` then PATH; **`ORG_INTENT_PATH`** must resolve **under harness root**; **`Escape-MdCell`** extended (backticks, `&lt;` / `&gt;`); org **values** bullets + goals **priority** escaped; [ENV_STANDARD.md](../../local-proto/docs/ENV_STANDARD.md) rows for **`POWERSHELL_EXE`** and tightened **`ORG_INTENT_PATH`**.
- **Follow-up hardening:** **`sync_harness_to_vault.ps1`** rejects vault root if it is a **reparse point** (when path exists); Org-Intent **Source** line uses **`Escape-MdCell`** (no inline backtick span around path); OBSIDIAN doc updated for reparse behavior.

## Next

- **Commit + push MiscRepos:** `local-proto/scripts/*.ps1`, `.cursor/scripts/vault_resync.py` + touched Python, `package.json`, `.cursor/docs/*.md`, `local-proto/docs/ENV_STANDARD.md`; run hooks.
- **Operator:** `npm run vault:sync` (or `int-vault-resync`) with **`OBSIDIAN_VAULT_ROOT`** / **`VAULT_SYNC_SAFE_BASE`** so **Harness/** picks up **Mirror-Status**, **.mirror-meta.json**, and script deltas. If vault root is a **junction/symlink**, sync now **errors** — use a normal directory or create the real vault path first.

## Todos — OpenGrimoire full review refresh (OA-FR-REFRESH-2026-04-17)

**Source:** [OpenGrimoire `docs/plans/OPENGRIMOIRE_FULL_REVIEW_REFRESH_2026-04-17.md`](../../../OpenGrimoire/docs/plans/OPENGRIMOIRE_FULL_REVIEW_REFRESH_2026-04-17.md) · harness row **`OA-FR-REFRESH-2026-04-17`** in [pending_tasks.md](Harness/Pending-Tasks.md) § OPENGRIMOIRE_FULL_REVIEW.

- [ ] **OG-REFRESH-01 (E2E / task success):** Playwright spec — dispatch `opengrimoire-survey-data-changed` (or real POST + moderation) and assert a **second** `GET` to `/api/survey/visualization` (closes REQ-S2.VIZ-REF observer gap).
- [ ] **OG-REFRESH-02 (docs / cognitive load):** One operator diagram: when to use **`/wiki`** vs vault SSOT — link [WIKI_MIRROR.md](../../../OpenGrimoire/docs/WIKI_MIRROR.md).
- [ ] **OG-REFRESH-03 (a11y):** BrowserStack `accessibilityExpert` on `/context-atlas` + `/visualization` when creds available; append rows per **BM-A11Y** in [BRAIN_MAP_AUDIT.md](../../docs/BRAIN_MAP_AUDIT.md).
- [ ] **OG-REFRESH-04 (lint / visual):** Policy for **ESLint `react-hooks/exhaustive-deps`** on Alluvial/Chord — Warn-tier backlog vs fix (see `npm run verify` warnings).
- [ ] **OG-REFRESH-05 (A2UI / OGAN-17):** Selector appendix — `vizLayoutIds`, moderation + viz `data-testid` → [AGENT_INTEGRATION.md](../../../OpenGrimoire/docs/AGENT_INTEGRATION.md).
- [ ] **OG-REFRESH-06 (agent parity / docs):** CONTRIBUTING note: use **`npm run test:e2e`** / **`npm run verify:e2e`** (not bare `npx playwright test` without `webServer`); [DEPLOY_AND_VERIFY.md](../../../OpenGrimoire/docs/engineering/DEPLOY_AND_VERIFY.md) already has the footgun line — cross-link from CONTRIBUTING if missing.

## Paths / artifacts

| Area | Path |
|------|------|
| Sync / wrapper | `MiscRepos/local-proto/scripts/sync_harness_to_vault.ps1`, `Sync-VaultHarness.ps1`, `int-vault-resync.ps1` |
| npm | `MiscRepos/package.json` |
| Python | `MiscRepos/.cursor/scripts/vault_resync.py`, `write_handoff.py`, `archive_handoff_latest.py`, `split_done_tasks_to_completed.py`, `validate_pending_tasks_table.py` |
| Docs | `MiscRepos/.cursor/docs/OBSIDIAN_VAULT_INTEGRATION.md`, `HANDOFF_FLOW.md`, `COMMANDS_README.md`, `MiscRepos/local-proto/docs/ENV_STANDARD.md` |

## dependency_links

- [OBSIDIAN_VAULT_INTEGRATION.md](../docs/OBSIDIAN_VAULT_INTEGRATION.md)
- [HANDOFF_FLOW.md](../HANDOFF_FLOW.md)
- [HARNESS_VAULT_WRITE_CONTRACT.md](../../local-proto/docs/HARNESS_VAULT_WRITE_CONTRACT.md)
- [OpenGrimoire OPENGRIMOIRE_FULL_REVIEW_REFRESH_2026-04-17.md](../../../OpenGrimoire/docs/plans/OPENGRIMOIRE_FULL_REVIEW_REFRESH_2026-04-17.md)

## open_risks

- **Reparse check** runs only when vault path **already exists**; brand-new vault directory is unchecked until created.
- **`LASTEXITCODE`** is cleared after **`git`** only; if a future native helper after that sets a non-zero code without throwing, wrappers could still mis-read exit — low risk with current post-git steps.

## Decisions / gotchas

- Do **not** end `sync_harness_to_vault.ps1` with **`exit`**: it is invoked via **`&`** from **`int-vault-resync.ps1`** (same process).

## Verification

- PowerShell parser on updated sync scripts; `python -m py_compile` on touched `.cursor/scripts/*.py`.
