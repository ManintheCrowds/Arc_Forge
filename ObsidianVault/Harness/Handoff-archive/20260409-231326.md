---
title: "Handoff #3: OpenAtlas → OpenGrimoire naming sweep"
tags: ["type/harness-state", "status/mirror", "domain/harness"]
group: harness-archive
color: slate
cssclasses:
  - vault-grp-harness-archive
  - vault-col-slate
---

# Handoff #3: OpenAtlas → OpenGrimoire naming sweep

**decision_id:** handoff-2026-04-03-opengrimoire-naming-migration  
**supersedes:** handoff-2026-03-30-skills-stack-agent-native  
**Updated:** 2026-04-06T12:00:00Z  
**latency_tolerance:** async_ok  

## Context

Cross-repo **naming migration**: replace legacy **OpenAtlas** product/folder references with **OpenGrimoire**, align **portfolio-harness** with the canonical app repo, and fix a corrupted migration script after a bulk replace touched its source.

## Done

- **Bulk text migration** (~230 files) across **OpenGrimoire**, **portfolio-harness**, **MiscRepos**, **OpenHarness**, **Arc_Forge**, **software** via `.cursor/scripts/migrate_strings_to_opengrimoire.py` (restore noted below).
- **Manual fixes:** Mermaid subgraph IDs (`openAtlas` → `openGrimoire`), “context atlas pages” → OpenGrimoire context graph wording in Arc_Forge + software plans/Obsidian note.
- **Renames:** Plan filenames, `SCOPE_OPENGRIMOIRE_*`, `2026-03-19-opengrimoire-*` plans, `opengrimoire_e2e.yml` (was `openatlas_e2e.yml`), trustgraph `merge_opencompass_stub_into_opengrimoire.*`, `OPENCOMPASS_OPENGRIMOIRE_INTEROP.md`, audit/scope/brainstorm docs with `openatlas` in names; Arc_Forge + software `*openatlas*.plan.md` → `*opengrimoire*`.
- **Layout:** **portfolio-harness:** removed broken `OpenAtlas` junction; added **`OpenGrimoire`** junction → `C:\Users\Dell\Documents\GitHub\OpenGrimoire`. **MiscRepos:** empty **`OpenAtlas`** folder renamed to **`OpenGrimoire`** (CI paths expect `OpenGrimoire/**`).
- **Script integrity:** `migrate_strings_to_opengrimoire.py` was **corrupted** when the bulk job replaced strings inside `transform()`; **restored** full `transform()` implementation.

## Next

**OpenClaw ↔ harness (2026-04-04):** Multi-file and repo-scale work stay in Cursor with bounded context per [OpenHarness `docs/CONTEXT_ENGINEERING.md`](../../../OpenHarness/docs/CONTEXT_ENGINEERING.md). OpenClaw is for short, gated async turns (workspace `SOUL.md`, [`local-proto/docs/TOOL_SAFEGUARDS.md`](../../local-proto/docs/TOOL_SAFEGUARDS.md))—not “whole repo in chat.” For phone/async continuation, use orchestrator-distilled **`next_prompt.txt`** and **SimpleX** (OpenClaw) per [`local-proto/docs/HITL_CONNECTIVITY.md`](../../local-proto/docs/HITL_CONNECTIVITY.md) and [OpenHarness `docs/HANDOFF_FLOW.md`](../../../OpenHarness/docs/HANDOFF_FLOW.md); orchestrator **Signal** is optional and off by default — not full session logs.

1. **Commit** in each touched repo (OpenGrimoire, MiscRepos, OpenHarness, Arc_Forge, software, portfolio-harness if tracked) with a clear message; stage `handoff_archive/*.md` + `handoff_latest.md` together per pre-commit rules.
2. **CI / layout:** Confirm **MiscRepos** `.github/workflows/opengrimoire_e2e.yml` matches your intent (nested `OpenGrimoire/` vs submodule vs separate repo checkout).
3. **Optional:** Run `npm run verify` (or `lint` + `type-check`) in **OpenGrimoire** if you want a clean bill of health after doc/API string edits.
4. **Grep hygiene:** `migrate_strings_to_opengrimoire.py` still contains literal `OpenAtlas` inside `transform()` by design; use composed strings only if you need zero `openatlas` substring matches in repo-wide scans.
5. **Async ledger → orchestrator:** Edits to **`OpenHarness/state/async_tasks.yaml`** alone do **not** trigger the MiscRepos orchestrator. When a task is **`blocked_hitl`** or the ledger changes materially, add one line here under **Next** (e.g. `HITL: <task-id> blocked_hitl — see OpenHarness state/async_tasks.yaml`) so the watched handoff updates and [`local-proto/docs/HITL_CONNECTIVITY.md`](../../local-proto/docs/HITL_CONNECTIVITY.md) flow applies.

## Paths / artifacts

| Area | Path |
|------|------|
| Migration script | `MiscRepos/.cursor/scripts/migrate_strings_to_opengrimoire.py` |
| E2E workflow | `MiscRepos/.github/workflows/opengrimoire_e2e.yml` |
| Portfolio junction | `portfolio-harness/OpenGrimoire` → `...\GitHub\OpenGrimoire` |
| Empty app dir (MiscRepos) | `MiscRepos/OpenGrimoire/` (renamed from OpenAtlas) |

## dependency_links

- (none for this pass)

## open_risks

- **Remote GitHub repo name** may still be legacy; local paths and docs now say OpenGrimoire—rename remote when ready.
- **Multi-root edits:** Verify no half-updated clone or fork still points at old `OpenAtlas` paths in private notes.

## Decisions / gotchas

- **Junction:** `portfolio-harness/OpenGrimoire` is a **directory junction** to the OpenGrimoire repo root; do not confuse with a nested copy.
- **Definition of done (this session):** Naming + layout + handoff; **no** full `npm run verify` or Playwright run executed after bulk edits.
- **OpenClaw `tinyllama`:** A raised `contextWindow` in `%USERPROFILE%\.openclaw\openclaw.json` satisfies OpenClaw’s minimum-context rule; **effective** context remains ~2k—treat long-thread reasoning as unsupported.

## Verification / ops report (OpenClaw, 2026-04-04)

**Versions:** Node v24.14.0, OpenClaw 2026.3.24 (cff6dc9), Ollama 0.19.0. **Primary model:** `ollama/tinyllama:latest`. **GPU:** `ollama ps` after a generate shows `tinyllama:latest` on **100% GPU** (context 2048 per Ollama).

| Check | Result |
|--------|--------|
| `GET http://127.0.0.1:11434/api/tags` | OK |
| Gateway `127.0.0.1:18789` | LISTENING |
| `openclaw health` (with `OPENCLAW_GATEWAY_TOKEN` from config; token not logged) | OK; SimpleX reported **not configured**; legacy SimpleX plugin loads with provenance warning |
| `openclaw agent --session-id … --message "…" --json` | CLI `status: ok` / `summary: completed` but `stopReason: error`—Ollama **400**: `tinyllama:latest` **does not support tools** (OpenClaw tool loop) |
| `openclaw pairing list` | No pending SimpleX pairing requests |
| SimpleX E2E (inbound → reply) | **Not verified** this run—requires operator pairing + channel configured |

**Merge-quality gate:** **TinyLlama via OpenClaw is not** a substitute for harness verification (tests/lint/build per [`HANDOFF_FLOW`](../../../OpenHarness/docs/HANDOFF_FLOW.md)); fix model/tooling or use a tool-capable model for full agent turns.

## OpenClaw gateway + primary model (2026-04-05, no secrets)

- **Gateway:** `openclaw gateway run --force`; `127.0.0.1:18789` **LISTENING**; restarted after config edits.
- **`openclaw health`:** OK (non-secret lines: SimpleX **not configured**, `Agents: main`, Simplex plugin provenance warning as before).
- **Ollama:** **0.20.2**; pulled **`llama3.2:3b`** (~2.0 GB on disk).
- **`agents.defaults.model.primary`:** **`ollama/llama3.2:3b`** (registry entry added in `openclaw.json` with `contextWindow` 131072 per OpenClaw model table).
- **Tool support (Ollama, verified):** `POST /api/chat` with a dummy tool — **no** “does not support tools” error; model responded (assistant `content` present, `done: true`).
- **GPU (`ollama ps` after generate):** `llama3.2:3b` — **16%/84% CPU/GPU**, ~3.1 GB resident (1050 Ti 4 GB).
- **Gateway token:** **Rotated** in `openclaw.json` after a nested-PowerShell mistake risked echoing the old value in logs; **reload** `OPENCLAW_GATEWAY_TOKEN` from file in any open shells—**never** paste token into chat.
- **`openclaw agent` JSON smoke:** Not fully captured in agent terminal output this run; re-run locally after confirming gateway picked up `primary` if needed.

## GPU swap — pre-swap baseline (no secrets)

Captured before replacing the inference GPU per [MiscRepos `docs/superpowers/specs/2026-04-04-openclaw-local-tools-cloud-gate-design.md`](../../docs/superpowers/specs/2026-04-04-openclaw-local-tools-cloud-gate-design.md) §F.1.

**Live OpenClaw/Ollama facts** (tool-capable primary, gateway health, token rotation) are authoritative in **[§ OpenClaw gateway + primary model (2026-04-05)](#openclaw-gateway--primary-model-2026-04-05-no-secrets)** above. The table below keeps **hardware + historical** snapshot for the swap decision; model rows labeled *at baseline capture* are **before** the 2026-04-05 `llama3.2:3b` remediation.

| Field | Value |
|--------|--------|
| **GPU (nvidia-smi)** | NVIDIA GeForce **GTX 1050 Ti**, 4096 MiB, WDDM, bus `00000000:03:00.0` |
| **Driver line (nvidia-smi header)** | **NVIDIA-SMI 560.94** — Driver Version **560.94**, CUDA Version **12.6** |
| **Ollama** (*at baseline capture*) | **0.19.0** — **current host:** **0.20.2** (see § OpenClaw gateway + primary model, 2026-04-05) |
| **`ollama list`** (*at baseline capture*) | `glm-4.7-flash:latest`, `tinyllama:latest` — **later:** `llama3.2:3b` pulled; **current `primary`:** `ollama/llama3.2:3b` (§ above) |
| **`agents.defaults.model.primary`** (*at baseline capture*) | Was `ollama/tinyllama:latest` — **superseded 2026-04-05** by **`ollama/llama3.2:3b`** (§ OpenClaw gateway + primary model) |
| **OpenClaw tool smoke** (*at baseline capture*) | **FAIL** on `tinyllama` (does not support tools) — **addressed** with `llama3.2:3b` + Ollama `/api/chat` tools check (§ above) |

**Orchestrator sync (2026-04-06):** `MiscRepos/.cursor/orchestrator_config.json` **`ollama_model`** set to **`llama3.2:3b`** to match OpenClaw `primary`.

**Rollback:** Keep the **current (1050 Ti) card available** for at least **one boot** if POST or driver bring-up fails on the new GPU.

**Bring-up:** **Pre-download** the NVIDIA **Game Ready or Studio** installer for the **new** GPU (matching this Windows build) **before** the swap so network issues do not block driver install.

## scope

Repo text, filenames, workflow name, portfolio junction, MiscRepos empty folder rename; not: GitHub org/repo rename, OpenGrimoire route URLs (e.g. `/context-atlas` left unchanged unless a separate pass).

## confidence_score

0.85 — broad sweep; user should commit and run project verification where it matters.

## OpenClaw end-to-end closure (2026-04-06)

- **SOUL.md:** `%USERPROFILE%\.openclaw\workspace\SOUL.md` merged with `local-proto/docs/SOUL_TEMPLATE.md` (HITL channel, OpenClaw trust-boundary `APPROVAL_NEEDED`, `SESSION_CONFIRM`). Template **Purpose** line now points at the **workspace** path.
- **Agent smoke:** `openclaw agent --session-id … --message "Say only: ok" --json` with `Tee-Object` → `%TEMP%\openclaw-agent-smoke.json` — **`stopReason` is `stop`** after lowering **`llama3.2:3b`** registry **`contextWindow` to 16000** (OpenClaw minimum; avoids Ollama reserving ~14 GiB system RAM from the prior 131072 setting).
- **Gateway persistence:** Scheduled task creation (`schtasks` ONLOGON) returned **Access denied** on this machine — fallback: **`%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\OpenClaw-Gateway.bat`** starts `openclaw gateway run --force` minimized at user logon. After reboot, confirm **`127.0.0.1:18789`** LISTENING without opening a terminal manually.
- **SimpleX E2E:** `openclaw pairing list` → no pending requests; `openclaw health` still reports **SimpleX: not configured** — operator should complete control UI / pairing / first inbound per `local-proto/docs/OPENCLAW_SIMPLEX_GO_LIVE.md` and re-check health (no secrets recorded here).
- **Simplex plugin WARN:** Keeping vendored **`%USERPROFILE%\.openclaw\extensions\simplex`** with **`plugins.allow`: `simplex`** — **accept** stderr provenance noise until a **backed-up** trial of `openclaw plugins install @dangoldbj/openclaw-simplex` per the same go-live doc.
## Navigation

Archived handoff snapshot (UTC filename). See [[Harness/MOC_Harness_State]].

Prev: [[Harness/Handoff-archive/20260403-214950.md]] | [[Harness/MOC_Harness_State|Handoff index]] | Next: [[Harness/Handoff-archive/20260409-231452.md]]
