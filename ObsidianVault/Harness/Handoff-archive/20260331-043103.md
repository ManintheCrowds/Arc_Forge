---
title: "Handoff (latest): OpenGrimoire GUI waves OA-2–OA-4 + portfolio harness"
tags: ["type/harness-state", "status/mirror", "domain/harness"]
---

# Handoff (latest): OpenGrimoire GUI waves OA-2–OA-4 + portfolio harness

**decision_id:** handoff-2026-03-30-opengrimoire-gui-waves-harness  
**Updated:** 2026-03-30  
**latency_tolerance:** async_ok

## Context

OpenGrimoire (**OpenGrimoire** product name; repo folder **OpenGrimoire**) GUI audit [`docs/audit/gui-2026-03-26.md`](../../../OpenGrimoire/docs/audit/gui-2026-03-26.md) — completed incremental waves via MiscRepos [`docs/audit/gui_wave_rollout_opengrimoire.yaml`](../../docs/audit/gui_wave_rollout_opengrimoire.yaml) with `python .cursor/scripts/weekly_gui_wave_prompt.py --config docs/audit/gui_wave_rollout_opengrimoire.yaml`.

## Done (this arc)

- **OA-2:** Operator intake in `SharedNavBar`; home card `data-testid` split; `e2e/smoke.spec.ts` covers nav from `/context-atlas`.
- **OA-3:** `e2e/brain-map.spec.ts` — `/brain-map` redirects to `/context-atlas` + shell parity.
- **OA-4:** `middleware.ts` gates `/test`, `/test-chord`, `/test-context`, `/test-supabase` in production unless `OPENGRIMOIRE_ALLOW_TEST_ROUTES`; `.env.example`; `e2e/test-routes.spec.ts` (dev webServer).
- Harness docs: `docs/audit/GUI_WAVE_TWO_LAYER_CONTRACT.md`, `NEW_GUI_SURFACE_CHECKLIST.md`, `--config` on weekly script.

## Review notes (critic-style)

- Edge middleware + env pattern looks sound; **drift risk:** new `app/test-*` routes must update `TEST_ROUTE_PREFIXES` and `middleware` `matcher` together.
- **Ops:** Staging should set `OPENGRIMOIRE_ALLOW_TEST_ROUTES` if those pages are needed; production should leave unset.
- **Coverage gap:** e2e only hits `/test`, not the other three prefixes (small follow-up for symmetry).

## Next steps

1. **Moltbook (default YAML):** [`docs/audit/gui_wave_rollout.yaml`](../../docs/audit/gui_wave_rollout.yaml) — **wave-2** still **open** (**MW-3** a11y baseline); run default `weekly_gui_wave_prompt.py` (no `--config`).
2. **OpenGrimoire:** All `oatlas-wave-*` in `gui_wave_rollout_opengrimoire.yaml` are **done** — add a new wave row in YAML when you pick the next GUI slice, or rely on [`agent_native_opengrimoire_2026-03-24.md`](../../../OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md) / `pending_tasks` for agent-native backlog.
3. **Optional tiny follow-ups:** Extend `test-routes.spec.ts` to smoke `/test-chord` (or one more prefix); add a one-line comment in `middleware.ts` pointing maintainers at the prefix + matcher checklist.

## Paths / artifacts

| Area | Path |
|------|------|
| OpenGrimoire GUI audit | `OpenGrimoire/docs/audit/gui-2026-03-26.md` |
| OpenGrimoire rollout YAML | `MiscRepos/docs/audit/gui_wave_rollout_opengrimoire.yaml` |
| Default portfolio YAML | `MiscRepos/docs/audit/gui_wave_rollout.yaml` |
| Weekly script | `MiscRepos/.cursor/scripts/weekly_gui_wave_prompt.py` |

## open_risks

- Middleware matcher must stay in sync with new dev-only App Router paths under `/test*`.

## scope

OpenGrimoire app + MiscRepos audit/harness docs; moltbook unchanged in last slice.

---

# Handoff #1: OpenCompass MCP allowlist and README follow-up

decision_id: handoff-20260324-opencompass-mcp-docs
Updated: 2026-03-24T00:00:00Z
Session: OpenCompass brain-map MCP allowlist + README handoff

## Context

Hardened `opencompass-brain-map-stub` merge targets with `OPENGRIMOIRE_BRAIN_MAP_ALLOW_PREFIXES` pointing at OpenGrimoire `public/`. Follow-up: user-level `C:\Users\Dell\.cursor\mcp.json` was not present on disk; workspace `MiscRepos/.cursor/mcp.json` carries the real path. Critic follow-up applied to **docs only** (example paths + reload note).

## Done

- Confirmed `MiscRepos/.cursor/mcp.json` `opencompass-brain-map-stub.env` includes `OPENGRIMOIRE_BRAIN_MAP_ALLOW_PREFIXES` for local OpenGrimoire `public/`.
- Updated `MiscRepos/.cursor/mcp/README.md`: placeholder paths in the `mcp.json` fragment, per-clone/machine note (absolute `public/`, no env interpolation in JSON strings), and **reload MCP or restart Cursor** after editing `env`.
- `/critic` on the allowlist bundle: pass; portability + ops gaps addressed in README.

## Next

1. **Operator:** Reload MCP servers in Cursor (or restart Cursor) so the stub process loads `OPENGRIMOIRE_BRAIN_MAP_ALLOW_PREFIXES` if not already done after the env change.
2. If your Cursor install uses **only** user-level MCP config, create or edit `C:\Users\Dell\.cursor\mcp.json` and duplicate the `opencompass-brain-map-stub` block (including the allow-prefix env) from `MiscRepos/.cursor/mcp.json`, then reload MCP again.
3. **Optional git:** Stage/commit `MiscRepos/.cursor/mcp/README.md` (and any pending `mcp.json` if you want it tracked).

**Verify:** After reload, invoke merge with `base_graph_path` under your allowed prefix; expect `path_not_allowed` JSON if pointed outside `OpenGrimoire/public`.

## Paths / artifacts

- `MiscRepos/.cursor/mcp/README.md` — example fragment, reload note, per-machine guidance
- `MiscRepos/.cursor/mcp.json` — live `opencompass-brain-map-stub` config (machine-specific paths)
- `MiscRepos/.cursor/mcp/opencompass_brain_map_stub_mcp.py` — `_brain_map_merge_path_allowed` + merge tool
- `MiscRepos/trustgraph-local-repo/interop/OPENCOMPASS_OPENGRIMOIRE_INTEROP.md` — contract reference

## dependency_links

- `MiscRepos/trustgraph-local-repo/interop/OPENCOMPASS_OPENGRIMOIRE_INTEROP.md`

## open_risks

- Committed `mcp.json` paths are host-specific; clones must edit `OPENGRIMOIRE_BRAIN_MAP_ALLOW_PREFIXES` (README now documents this).

## Decisions / gotchas

- Merge allowlist applies to **`opencompass_merge_stub_into_brain_map`** only (`base_graph_path`); CSV stub tool is unrestricted by prefix.
- README example uses `C:/path/to/...` placeholders; do not copy blindly into Cursor without replacing paths.

## scope

MiscRepos `.cursor/mcp` config and documentation; no OpenGrimoire app code changes in this thread.

---

# Handoff #2: OpenGrimoire Part A — agent-native audit follow-on (2026-03-24)

**decision_id:** handoff-2026-03-24-opengrimoire-part-a-next  
**latency_tolerance:** async_ok  
**human_gate:** none for doc-only; **human review** if changing alignment promotion rules or graph write APIs.

## Context

OpenGrimoire audit **`OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md`** — Part A work backlog is the source of truth for next actions. Part A includes a **work backlog table** (principles 1–8) plus cross-cutting **P0** visualization/schema (AC2). Scores in the summary table were left unchanged; execution is driven by the backlog rows.

**Synced to:** `MiscRepos/.cursor/state/pending_tasks.md` — new IDs **OA-OG-P0** through **OA-OG-5** under `PENDING_OPENGRIMOIRE_HARNESS`.

## Done (prior session)

- Audit doc: Part A backlog table; §7 capability discovery notes; P0 visualization callout vs `BrainMapGraph.tsx` / `BRAIN_MAP_SCHEMA.md`.
- `OpenGrimoire/src/app/capabilities/page.tsx`: in-app copy links to `docs/agent/INTEGRATION_PATHS.md` and `docs/OPERATOR_GUI_RUNBOOK.md`.

## Next (pick one vertical slice per session)

1. **P0 — OA-OG-P0:** Implement optional graph columns or detail drawer for schema fields **or** explicit deferral paragraph in `scope_opengrimoire_mvp_agent_native.md` (closes AC2 honestly).
2. **Docs — OA-OG-1:** Add curl + CLI examples to `docs/AGENT_INTEGRATION.md`; reference thin MCP stance from `docs/agent/INTEGRATION_PATHS.md`.
3. **UI — OA-OG-2:** Optional React Query/SWR on `src/app/admin/alignment/page.tsx`.
4. **Test — OA-OG-4 / OA-OG-5:** Playwright for capabilities JSON; A2UI on capabilities page if in scope.

**Do not** expand brain-map to POST without scope doc + AC row first (audit principle 5).

## Paths / artifacts

| Artifact | Path (from portfolio: sibling `OpenGrimoire/`) |
|----------|-----------------------------------------------|
| Audit + backlog | `OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md` |
| MVP scope / AC2 | `OpenGrimoire/docs/scope_opengrimoire_mvp_agent_native.md` |
| Schema | `OpenGrimoire/docs/BRAIN_MAP_SCHEMA.md` |
| Table UI | `OpenGrimoire/src/components/BrainMap/BrainMapGraph.tsx` (~436–476) |
| Capabilities | `OpenGrimoire/src/app/api/capabilities/route.ts`, `OpenGrimoire/src/app/capabilities/page.tsx` |
| Agent entry | `OpenGrimoire/docs/AGENT_INTEGRATION.md` |
| Pending tasks | `MiscRepos/.cursor/state/pending_tasks.md` (OA-OG-*) |

## Verification

- After TS/React changes: `npm run lint` in OpenGrimoire (and `npm run verify` if touching tests).
- After doc-only: spot-check relative links from audit to scope + schema.

## Decisions / gotchas

- **Prompt-native score 2/10** is intentional; do not “fix” without product scope change.
- **Context injection 3/8** is acceptable per `ARCHITECTURE_REST_CONTRACT.md` non-goals.
- **Thin MCP:** must not duplicate REST logic — see `INTEGRATION_PATHS.md`.

---

# Handoff #3: OpenGrimoire Part A — implementation complete (2026-03-24)

**decision_id:** handoff-2026-03-24-opengrimoire-part-a-shipped  
**latency_tolerance:** async_ok  
**human_gate:** none for this slice (already merged in workspace).

## Context

Continued **Handoff #2** (OpenGrimoire Part A). Source of truth: `OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md` (revision line documents post-P0 work).

## Done (this slice)

| ID | Outcome |
|----|---------|
| **OA-OG-P0** | AC2: `BrainMapGraph` Table shows **conditional columns** (Trust score, Compass axis, Grimoire tags, Insight level) when any visible node includes those fields; graph tooltips extended; `scope_opengrimoire_mvp_agent_native.md` AC2 + baseline updated. |
| **OA-OG-1** | `docs/AGENT_INTEGRATION.md`: **curl** examples for capabilities, brain-map graph, alignment list/create; CLI section unchanged in spirit. |
| **OA-OG-3** | Nav + footer already expose `/capabilities`; harness row marked done (ongoing: update `capabilities/route.ts` with API PRs). |
| **OA-OG-4** | `e2e/fixtures/brain-map-state-only.json` includes optional fields; `e2e/context-atlas.spec.ts` asserts columns; `e2e/capabilities.spec.ts` asserts `GET /api/capabilities` shape (`app`, `routes`). |
| **Portfolio** | `MiscRepos/.cursor/docs/TROUBLESHOOTING_AND_PLAYBOOKS.md` links `OpenGrimoire/docs/MONITORING_OPENGRIMOIRE.md` for app vs portfolio monitoring. |

**Deferred (not in slice):** **OA-OG-2** (React Query/SWR on `/admin/alignment`), **OA-OG-5** (A2UI on `/capabilities`) — see `pending_tasks.md` rows.

## Next agent

1. **Optional product:** Pick up **OA-OG-2** or **OA-OG-5** only if scope explicitly asks; otherwise leave deferred.
2. **Re-audit:** Audit top-10 #10 — optionally refresh Part A **summary score table** after human confirms P0 UI/schema story (scores were frozen by policy; prose already updated in § visualization).
3. **Discipline:** On every new `src/app/api/**` route, extend `src/app/api/capabilities/route.ts` in the same PR.

**Constraints (unchanged):** No brain-map POST without scope + AC updates. Thin MCP = HTTP wrappers only (`docs/agent/INTEGRATION_PATHS.md`).

## Verification (ran)

- OpenGrimoire: `npm run verify`; Playwright: `e2e/context-atlas.spec.ts` + `e2e/capabilities.spec.ts`.

## Paths / artifacts

- `OpenGrimoire/src/components/BrainMap/BrainMapGraph.tsx` — optional columns + types
- `OpenGrimoire/docs/AGENT_INTEGRATION.md` — curl block
- `OpenGrimoire/e2e/capabilities.spec.ts`, `e2e/context-atlas.spec.ts`, `e2e/fixtures/brain-map-state-only.json`
- `OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md` — revision + P0 §
- `MiscRepos/.cursor/state/pending_tasks.md` — OA-OG-P0/1/3/4 **done**; OA-OG-2/5 **deferred**

## dependency_links

- `OpenGrimoire/docs/audit/agent_native_opengrimoire_2026-03-24.md`
- `MiscRepos/.cursor/state/pending_tasks.md` § PENDING_OPENGRIMOIRE_HARNESS

## open_risks

- None introduced by this slice; deferred OA-OG-2/5 remain backlog if product wants richer admin cache or A2UI discovery.
## Navigation

Archived handoff snapshot (UTC filename). See [[Harness/MOC_Harness_State]].

Prev: (none) | [[Harness/MOC_Harness_State|Handoff index]] | Next: [[Harness/Handoff-archive/20260403-214950.md]]
