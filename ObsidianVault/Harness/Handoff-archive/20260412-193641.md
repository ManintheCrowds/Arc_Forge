---
title: "Handoff: Agent-native audit top-10 follow-ups (security + harness hygiene)"
tags: ["type/harness-state", "status/mirror", "domain/harness"]
---

# Handoff: Agent-native audit top-10 follow-ups (security + harness hygiene)

decision_id: handoff-2026-04-12-agent-native-top10
supersedes: handoff-2026-04-11-openclaw-topology-ops
Updated: 2026-04-12T22:30:00Z
Session: MiscRepos — implement agent-native_top-10_fixes plan (docs, PS1, Python, rules, state README)

## Context

Implemented the **attached audit follow-up plan** (no edits to plan files under workspace `.cursor/plans/`): Task Scheduler doc ordering + `/tr` warnings, `audit_wrapper` Windows shell hardening + tests, digest/vault path containment, migration CLI, GUI wave config containment, `-LiteralPath` on scheduled scripts, `session-bootstrap.mdc`, README / AGENT_ENTRY_INDEX discovery fixes, MCP map rows for Obsidian PS1 scripts, state README CRUD table.

## Done

- **Docs:** `SCHEDULED_TASKS.md` (Register-ScheduledTask first; schtasks warning); `AI_TRENDS_MCP.md` (Windows schtasks security note).
- **`audit_wrapper.py`:** Resolve `npx`/`uvx` via `shutil.which` when possible (`shell=False`); gated `shell=True` only for bare name + argv safety; `test_audit_wrapper_argv.py` (7 tests).
- **PS1:** `collect_weekly_digest.ps1` — `harness_output` must stay under harness root; `run_weekly_review.ps1` — vault copy paths must stay under `vault_root`; `scheduled_governance` / `scheduled_code_review` / `scheduled_perf_context_bundle` — `Test-Path -LiteralPath`.
- **`migrate_strings_to_opengrimoire.py`:** Required `--root` (repeatable), `--dry-run`, `--max-files`; removed hardcoded `ROOTS` (**breaking** for old zero-arg invocations).
- **`weekly_gui_wave_prompt.py`:** `load_config` rejects `--config` paths outside `repo_root` (`relative_to`).
- **Rules:** New `.cursor/rules/session-bootstrap.mdc` (`alwaysApply`); `.cursorrules` pointer under Memory & context.
- **Discovery:** `README.md` (USER_GUIDE link + Portfolio highlights anchors); `AGENT_ENTRY_INDEX.md` (internal `#portfolio-highlights` / `#skills-in-action`; new Skills subsection).
- **`MCP_CAPABILITY_MAP.md`:** “Harness Obsidian vault scripts (terminal only)” table (Lint / Scan / Add frontmatter).
- **`state/README.md`:** “Append-mostly ledgers (CRUD expectations)” subsection.

## Next

1. **Commit** MiscRepos when ready — review diff breadth (`audit_wrapper`, scripts, many docs).
2. **Operators:** Re-run `migrate_strings_to_opengrimoire.py` with explicit `--root …` (and `--dry-run` first) if you still use that migration.
3. **Optional:** `sync_harness_to_vault.ps1` if mirroring state/docs to Obsidian.

## Paths / artifacts

| Path | Notes |
|------|--------|
| `local-proto/docs/SCHEDULED_TASKS.md` | Harness-GovernanceRuns CLI order + schtasks warning |
| `local-proto/docs/AI_TRENDS_MCP.md` | schtasks security blurb |
| `local-proto/scripts/audit_wrapper.py` | Shell / argv safety |
| `local-proto/tests/test_audit_wrapper_argv.py` | New tests |
| `.cursor/scripts/collect_weekly_digest.ps1` | harness_output containment |
| `.cursor/scripts/run_weekly_review.ps1` | Vault path containment |
| `.cursor/scripts/migrate_strings_to_opengrimoire.py` | argparse CLI |
| `.cursor/scripts/weekly_gui_wave_prompt.py` | Config path containment |
| `.cursor/scripts/scheduled_governance.ps1` / `scheduled_code_review.ps1` | LiteralPath |
| `local-proto/scripts/scheduled_perf_context_bundle.ps1` | LiteralPath |
| `.cursor/rules/session-bootstrap.mdc` | New alwaysApply rule |
| `.cursorrules` | session-bootstrap pointer |
| `README.md`, `.cursor/docs/AGENT_ENTRY_INDEX.md` | Discovery |
| `.cursor/docs/MCP_CAPABILITY_MAP.md` | Obsidian PS1 terminal table |
| `.cursor/state/README.md` | CRUD / append-mostly |

## dependency_links

- [local-proto/docs/OBSIDIAN_GITHUB_GAP_ANALYSIS.md](local-proto/docs/OBSIDIAN_GITHUB_GAP_ANALYSIS.md) (referenced from MCP map)
- Plan reference (do not edit): workspace `agent-native_top-10_fixes_e66c968a.plan.md` — implementation completed in repo only

## open_risks

- **`migrate_strings_to_opengrimoire.py`** — scripts/docs that invoked it without args need updating to `--root`.
- **Bare `npx`/`uvx` on Windows** — if resolution fails and argv is “safe”, shell fallback still uses `cmd.exe` (narrower than before).

## Verification

- `python -m pytest local-proto/tests` — **47 passed** (includes new audit_wrapper argv tests).
- Smoke: `scheduled_governance.ps1 -SkipGuiWavePrompt` — **PASS** after PS1 edits.
## Navigation

Archived handoff snapshot (UTC filename). See [[Harness/MOC_Harness_State]].

Prev: [[Harness/Handoff-archive/20260412-193605.md]] | [[Harness/MOC_Harness_State|Handoff index]] | Next: [[Harness/Handoff-archive/20260414-010642.md]]
