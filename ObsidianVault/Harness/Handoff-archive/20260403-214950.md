---
title: "Handoff #2: Skills stack wave + agent-native audit snapshot"
tags: ["type/harness-state", "status/mirror", "domain/harness"]
---

# Handoff #2: Skills stack wave + agent-native audit snapshot

**decision_id:** handoff-2026-03-30-skills-stack-agent-native  
**supersedes:** (archived prior `handoff_latest.md` — GUI waves / OpenGrimoire OA-2–4 handoff block)  
**Updated:** 2026-03-30T12:00:00Z  
**latency_tolerance:** async_ok  

## Context

MiscRepos **skills stack improvement wave** (reflection → agent-first routing, handoff YAML, tiers, TEST_BASKET, qa-verifier regression section) is implemented in-repo. Separately, an **Agent-Native Architecture audit** was run against **OpenGrimoire** (eight principles, parallel explore + critic + security-sentinel); scores are directional—see open_risks for methodology caveats.

## Done

- **Phase A–D (MiscRepos):** Extended [`portable-skills/docs/IMPROVING_SKILLS.md`](../../portable-skills/docs/IMPROVING_SKILLS.md) (§7–15: agent-first, handoff contract, tiers, formatter hygiene, regression baskets); added [`portable-skills/docs/private-templates/skill-test-basket.md`](../../portable-skills/docs/private-templates/skill-test-basket.md); [`qa-verifier` SKILL](../skills/qa-verifier/SKILL.md) **Skill regression basket** section.
- **Pilot skills:** `product-scope`, `planning`, `meta-review` — `tier: 2`, `upstream_inputs` / `downstream_output` / `handoff_format`, **Agent routing** sections, [`TEST_BASKET.md`](../skills/product-scope/TEST_BASKET.md) per skill; synced to [`portable-skills/skills/`](../../portable-skills/skills/) (incl. new `meta-review/`).
- **Tooling / ops:** [`.prettierignore`](../../.prettierignore) for `SKILL.md`; [`validate_skill_frontmatter.py`](../scripts/validate_skill_frontmatter.py); [`COMMANDS_README.md`](../docs/COMMANDS_README.md) row; [`AI_TRENDS_SCP_PIPELINE.md`](../docs/AI_TRENDS_SCP_PIPELINE.md); [`GOVERNANCE_RITUAL.md`](../docs/GOVERNANCE_RITUAL.md) §6b skills check; meta-review **Checks** bullet for skill-wave drift.
- **Agent-native audit :** Consolidated report: strong capability discovery + thin CLI primitives; low packaged MCP parity; CRUD strict 0/5 entities; UI mostly request–response; security-sentinel **medium** risk if env flags mis-set.

## Next

1. **Before commit** (if staging rules/skills): `python .cursor/scripts/checksum_integrity.py --update` after human review of skill changes; then `checksum_integrity.py --verify --strict` on CI/pre-commit as you already do.
2. **Optional:** Run `python .cursor/scripts/validate_skill_frontmatter.py` (warnings OK; `--strict` fails until legacy YAML quoting fixed on e.g. `blue-hat-bitcoin` SKILL).
3. **OpenGrimoire backlog (from audit):** Thin MCP or expand `alignment-context-cli` for route parity; optional `GET /api/alignment-context/[id]`; `/help` linking USAGE + capabilities; rate limits on alignment APIs if automation increases.

## Paths / artifacts

| Area | Path |
|------|------|
| Skills docs | `MiscRepos/portable-skills/docs/IMPROVING_SKILLS.md` |
| Pilot TEST_BASKET | `MiscRepos/.cursor/skills/{product-scope,planning,meta-review}/TEST_BASKET.md` |
| MCP / ingest runbook | `MiscRepos/.cursor/docs/AI_TRENDS_SCP_PIPELINE.md` |
| Agent-native audit target | `OpenGrimoire/docs/AGENT_INTEGRATION.md`, `OpenGrimoire/src/app/api/capabilities/route.ts` |
| Plan (reference only) | `Arc_Forge/.cursor/plans/skills_stack_gap_wave_e4e0e1b6.plan.md` |

## dependency_links

- [GUI_WAVE_TWO_LAYER_CONTRACT.md](../../docs/audit/GUI_WAVE_TWO_LAYER_CONTRACT.md) — session contract pattern aligned with skill handoff fields.

## open_risks

- **Audit scores** mix rubrics (HTTP vs MCP, LLM context vs machine surfaces); v2 audit should add an evidence table per principle (critic: traceability + unified lens).
- **Integrity baseline:** Skill edits may require `--update` to `.cursor/private/integrity_baseline.json` when committing MiscRepos.

## Decisions / gotchas

- **Tiered skills** require `upstream_inputs`, `downstream_output`, `handoff_format` when `tier` is set—validator enforces.
- **OpenGrimoire** is intentionally **no in-app LLM**; “prompt-native 0%” is by architecture, not failure.

## scope

MiscRepos skills/docs/scripts + handoff references OpenGrimoire audit only; no OpenGrimoire code changed in the skills wave.
## Navigation

Archived handoff snapshot (UTC filename). See [[Harness/MOC_Harness_State]].

Prev: [[Harness/Handoff-archive/20260331-043103.md]] | [[Harness/MOC_Harness_State|Handoff index]] | Next: [[Harness/Handoff-archive/20260409-231326.md]]
